RewriteEngine On

# Se il file o la cartella richiesti esistono fisicamente (es. CSS, JS, immagini), mostrali normalmente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Altrimenti, ignora il percorso fisico e invia tutto a index.php
RewriteRule ^(.*)$ index.php [QSA,L]